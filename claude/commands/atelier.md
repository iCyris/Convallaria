# Atelier

Use `skills/atelier/SKILL.md` to route this design task. Classify the request, load only the relevant reference file or subskill, and produce the requested deliverables with an asset manifest when multiple files are generated.

